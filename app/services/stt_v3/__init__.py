"""Вендоренные gRPC-стабы Yandex SpeechKit STT v3 (только стриминг Recognizer).

Сгенерированы из официальных proto (yandex-cloud/cloudapi) через grpcio-tools
1.69 / protobuf 5.29 — версии совпадают с requirements.txt, поэтому пересборка
образа не требуется (файлы монтируются через ./app).
"""
